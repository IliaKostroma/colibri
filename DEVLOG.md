# DEVLOG — COLIBRI

Журнал стройки. Новые записи — **внизу**. Формат: дата, кто (модель/роль), что сделал, решения, что осталось.
Нетехническое (продукт, ценность, монетизация) — в `STRATEGYLOG.md`.

---

## Пред-история (реконструкция по git, до заведения журнала)

Восстановлено из истории репозитория `github.com/IliaKostroma/colibri` — 9 коммитов, дек 2025 — янв 2026.

- **2025-12-23** `49d2534` — первый релиз COLIBRI: таск-менеджер с голосовым вводом. До этого проект назывался Voice Transcriber (следы остались в `.kiro/specs/`, `DESIGN-SYSTEM.md`, `build.appId = com.voicetranscriber.app`, старых сборках в `release/`).
- **2025-12-23** `e0f3872` — PWA-поддержка, кэширование авторизации (`colibri_auth_cache`), обработка ошибок.
- **2025-12-23** `389c0f5` — переход на offline-first: локальный кэш задач + фоновая синхронизация.
- **2025-12-23** `389c778` — диагностические инструменты в настройках.
- **2025-12-24** `a69d4b7` — прокрутка модалки настроек на мобильном.
- **2026-01-19** `a385d17` — оптимизация производительности через offline-first.
- **2026-01-20** `4c19629` — таймауты (`withTimeout`, 8 с) против вечной загрузки и зависшего синка.
- **2026-01-20** `60f6896`, `47381ee` — триггер прод-деплоя, убран индикатор синхронизации из UI.

Затем ~6 месяцев без коммитов. В рабочей копии накопились незакоммиченные изменения (см. запись 2026-07-27).

---

## 2026-07-27 — Claude Opus 5 (аудит + переезд в волт)

**Что просил Илья:** проверить проект, затем перенести его в MotherVault по аналогии с positive-search — с инструкцией для агентов, девлогом и прочим.

### Аудит

Что работает:
- `npm run build` проходит: 48 модулей, 208 kB (56 kB gzip), 0.4 с.
- Секретов в коде и в бандле нет. В `dist/assets/index-*.js` только Supabase **anon**-ключ (`"role":"anon"`) — by design. `.env` в `.gitignore` и никогда не был в истории git.
- Electron настроен безопасно: `nodeIntegration: false`, `contextIsolation: true`.

Главная находка — **тесты мёртвые, и это замаскировано**:
- `supabase.js:15` дёргает `window.matchMedia` на уровне модуля, в `test-setup.js` стаба нет → `storage.test.js` и `import-dates.test.js` падают на импорте. Vitest показывает «41 passed / 2 файла упало», 27 тестов внутри не считаются.
- Со стабом `matchMedia` собирается 68 тестов, **17 падают**: тесты проверяют синхронный localStorage-API, а `storage.js` давно асинхронный и ходит в Supabase. То есть `storage.js` (829 строк) — без покрытия. Стаб проверял временно, откатил.

Прочее:
- Незакоммиченные изменения (5 файлов) — не просто рефакторинг: OpenRouter переведён с пресетов `@preset/bettertext` / `@preset/tran-sto-eng` на произвольную модель (дефолт `google/gemini-2.0-flash-001`) + свои системные промты, в UI добавлено поле «Модель», подпись «OpenRouter (бесплатно)» убрана. **Это смена биллинга.** Локально, в прод не ушло (`origin/main` = `47381ee`).
- Два вопроса к базе, отсюда не проверяемые: есть ли колонка `openrouter_model` в `user_settings` и есть ли UNIQUE на `user_settings.user_id` (иначе `upsert` без `onConflict` плодит строки и `.single()` начнёт падать). → `T2`, `T3`.
- `src/.DS_Store` закоммичен вопреки `.gitignore`; `release/` содержит сборки под мёртвым именем «Voice Transcriber 1.0.0»; README нет; `.kiro/specs/` разошлась с кодом.

### Переезд

- `git gc --prune=now`: `.git` **347 МБ → 260 КБ** (3718 распакованных объектов, ноль паков, висячий мусор). История цела — 9 коммитов, рабочий диф не тронут.
- `mv /Users/ilakostroma/Documents/KIROTEST` → `MotherVault/colibri`. Старого пути больше нет.
- **Git-топология:** проект остаётся самостоятельным репозиторием со своим origin. В `.gitignore` волта добавлена строка `colibri/` — волт папку не трекает. Причина: `colibri/` приехал с готовым `.git`, и `git add` из корня волта создал бы gitlink-заглушку (в отличие от `positive-search/`, где файлы попали в индекс волта до появления внутреннего репозитория). Netlify-деплой и GitHub при этом целы. **Коммитить надо изнутри `colibri/`.**
- `node_modules` (653 МБ) и `release` (208 МБ) переехали как есть — по решению Ильи. Итого ~863 МБ внутри синкаемого волта. Если Obsidian начнёт задыхаться — Settings → Files & Links → Excluded folders, либо снести и вернуть через `npm ci`.
- Заведён комплект документации: `AGENTS.md`, `DEVLOG.md`, `STRATEGYLOG.md`, `PROJECT_STATE.json`, `DEPLOYMENT.md`, `COLIBRI — идея проекта.md`.

**Что осталось (в порядке приоритета):** `T1` починить тесты, `T2`/`T3` проверить схему Supabase, `T4` решить судьбу незакоммиченного OpenRouter-изменения. Детали — в `PROJECT_STATE.json`.

**Код не менялся** — только `git gc`, перемещение папки и новые `.md`-файлы. Рабочий диф из 5 файлов остался нетронутым.

---

## 2026-07-27 (вечер) — Claude Opus 5 (подключение к Netlify)

Илья: «не хочу сам туда ходить» — подключаем деплой-контур без похода в веб-консоль.

- `netlify-cli@27` поставлен **локально в проект** (`npm install --no-save`), не глобально. `package.json` и `package-lock.json` не тронуты, `node_modules` и так в `.gitignore`.
- Логин — единственное, что потребовало человека: CLI выдал ticket-ссылку, Илья подтвердил в браузере одним кликом. Токен лёг в системный конфиг Netlify CLI, не в репозиторий.
- `netlify link --name c0libri` — папка привязана к сайту. CLI сам дописал `.netlify` в `.gitignore` проекта (поэтому `.gitignore` теперь в диффе).

**Что выяснилось (всё это раньше было в «Проверить»):**

- Сайт — **`c0libri`**, https://c0libri.netlify.app, отвечает 200. Аккаунт `sdby.iliavladimirovich@gmail.com`.
- Сборка: репозиторий `IliaKostroma/colibri`, ветка `main`, `npm run build` → `dist`. **Автодеплой включён** (`stop_builds: false`), опубликован `47381ee` от 20.01.2026, статус `ready`. То есть live ровно то, что лежит в `main`.
- Переменные окружения в контексте `production` **заданы обе** и указывают на тот же проект Supabase (`yzmbqchwbjlnbmoytfes`), что и локальный `.env`.

**Новый вопрос, которого раньше не было:** прод и локальная разработка ходят в **одну базу**. Правки на localhost сразу видны в бою, а неудачная миграция ляжет на боевые задачи. Вынесено в `openQuestions`.

Разделы 0 и 4 в `DEPLOYMENT.md` обновлены под факты, Netlify-пункт из «Проверить» вычеркнут. Осталось три вопроса к Supabase — их CLI не видит, нужен доступ к базе.

**Код по-прежнему не менялся.**

---

## 2026-07-27 (вечер, 2) — Claude Opus 5 (JSON-бэкап задач + фикс кэша при импорте)

**Контекст.** Прод и локальная разработка ходят в одну базу Supabase. Илья: перед пушем можно сохранять задачи в `.md`, функция уже есть. Разбор показал, что как страховка она дырявая — отсюда задача.

### Почему markdown не годится для бэкапа

`exportTasksToMarkdown()` теряет при круге «экспорт → импорт»:
- **переносы строк** — `task.text.replace(/[\r\n]+/g, ' ')`, многострочная задача схлопывается навсегда;
- **время суток** — дата пишется как ДД.ММ.ГГГГ, импорт восстанавливает полночь (`new Date(year, month, day)`);
- **дату создания у выполненных задач** — в md уходит только дата выполнения, и на импорте `createdAt = completedAt` (`storage.js:783`).

При этом `exportTasks()`/`importTasks()` — JSON, `version: 1`, полный объект — были написаны, но **не подключены к UI**: ни кнопки, ни обработчика. Мёртвый код.

### Что сделано

- **Подключён JSON-бэкап.** В `index.html` — два новых элемента управления (`export-json-btn`, `import-json-btn`) плюс скрытый `json-file-input`, отдельным рядом над markdown-кнопками. В `app.js` — `exportJsonFile()` / `importJsonFile()` по образцу markdown-обработчиков, файл называется `colibri-backup-<timestamp>.json`. Все четыре точки по `AGENTS.md` пройдены: объявление в `elements` → `cacheElements()` → `bindEvents()` → сами функции.
- **Тексты в UI переписаны честно:** «Бэкап (.json) — точная копия для восстановления. Слепок (.md) — для чтения в заметках», и прямым текстом сказано, что теряет markdown.
- **Починен баг, найденный по дороге.** `importTasks()` и `importTasksFromMarkdown()` писали в Supabase, но **не обновляли локальный кэш**, а UI читает именно из кэша (`getTasks()` → `getCachedTasks()`). То есть после «восстановить» пользователь до 15 секунд (до следующего фонового синка) смотрел на старые задачи и мог решить, что импорт не сработал. Добавлен `setCachedTasks(tasksToSave)` в обе ветки. Баг был и в markdown-импорте — то есть в том самом сценарии, на который Илья рассчитывал как на страховку.

### Проверка

- Новый `src/services/backup-json.test.js` — 6 тестов, все зелёные: валидность экспорта, круг без потерь, сохранность именно того, что теряет markdown (переносы / время / `createdAt` у выполненных), обновление кэша, режим `merge`, устойчивость к мусору вместо JSON. Стаб `matchMedia` положен **внутрь файла**, `test-setup.js` не тронут — чтобы не смешивать с `T1`.
- `npm test`: 47 тестов зелёных (было 41), два устаревших сьюта по-прежнему падают на сборе — это `T1`, не трогали.
- `npm run build` проходит, бандл 210 kB (56.6 kB gzip).
- Dev-сервер: новые кнопки реально отдаются в HTML, обработчики в бандле.
- **Превью-деплой:** https://backup-json--c0libri.netlify.app — отвечает 200, кнопки на месте. Прод остался на `47381ee`, не тронут.

### Осторожно

Превью собиралось из рабочей копии, поэтому в него попало и незакоммиченное OpenRouter-изменение (`T4`). В коммит оно **не пошло**: индекс собран вручную (`git hash-object` + `git update-index`), чтобы смена биллинга не уехала в прод вместе с бэкапом. `T4` так и висит в рабочей копии и ждёт ответа по `T2`.
